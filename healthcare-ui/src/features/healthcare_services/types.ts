export interface ServiceCard {
  icon: string;
  title: string;
  description: string;
  action: string;
  iconBg: string;
  iconColor: string;
  accentColor: string;
}

export interface LiveResult {
  icon: string;
  name: string;
  meta: string;
}
